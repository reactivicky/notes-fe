import { NavContainer, NavText } from "./styles";

const Navbar = () => {
  return (
    <NavContainer>
      <h1>Notes App</h1>
      <NavText>Take notes and never forget</NavText>
    </NavContainer>
  );
};

export default Navbar;
